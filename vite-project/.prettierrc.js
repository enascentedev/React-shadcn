module.exports = {
	useTabs: true,             // Usa tabulação em vez de espaços para identação
	semi: true,                // Adiciona ponto e vírgula no final de cada linha
	tabWidth: 2,               // Define a largura da tabulação como 2 espaços (caso useTabs esteja false)
	singleQuote: false,        // Usa aspas duplas em vez de aspas simples
	trailingComma: "es5",      // Adiciona vírgulas finais apenas em objetos e arrays (conforme o ES5)
	printWidth: 100,           // Define a largura máxima de uma linha antes de quebrar
	endOfLine: "auto"          // Usa o tipo de quebra de linha do sistema operacional atual (evita conflitos entre SOs)
};
